/**
 * @fileoverview pattern-engine — universal drum-pattern + song-arrangement
 * generator. Produces a device-agnostic song structure (roles, steps, velocity,
 * micro-timing, bars, repeats, meter). Pack it for a target device with a
 * builder package.
 */
export * from './engine.js';
export * as engine from './engine.js';
